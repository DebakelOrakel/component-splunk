parameters:
  kapitan:
    compile:
      - input_paths:
          - splunk/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - splunk/component/main.jsonnet
        input_type: jsonnet
        output_path: splunk/

